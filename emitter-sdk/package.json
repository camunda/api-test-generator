{
  "name": "@camunda8/emitter-sdk",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Stable contract package for materialization emitters in api-test-generator. Implement EmitterStrategy and register the implementation through this SDK to plug a new output target (Playwright spec, SDK suite, etc.) into the materialization pipeline.",
  "main": "./dist/src/index.js",
  "types": "./dist/src/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "default": "./dist/src/index.js"
    },
    "./types": {
      "types": "./dist/src/types.d.ts",
      "default": "./dist/src/types.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist || rm -rf dist"
  },
  "dependencies": {
    "path-analyser": "*"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
